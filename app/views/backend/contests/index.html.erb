<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "#{t("web-app-theme.list", :default => "List")}", backend_scope_contests_path(@scope)  %></li>
      <li><%= link_to "#{t("web-app-theme.new", :default => "New")}", new_backend_scope_contest_path(@scope) %></li>
    </ul>
  </div>          
  <div class="content">          
    <h2 class="title"><%= t("web-app-theme.all", :default => "All")  %> Contests </h2>
    <div class="inner">
      <table class="table">
        <tr>             
          <th class="first">ID</th>
                    <th>
            <%= t("activerecord.attributes.contest.name", :default => t("activerecord.labels.contest", :default => "Name")) %>
          </th>
                    <th><%= t("web-app-theme.created_at", :default => "Created at")  %></th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @contests.each do |contest| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= contest.id %>
          </td>
                    <td>
            <%= link_to contest.title, backend_scope_contest_path(@scope, contest) %>
          </td>
                    <td>
            <%= contest.created_at %>
          </td>
          <td class="last">
            <%= link_to "#{t("web-app-theme.show", :default => "Show")}", backend_scope_contest_path(@scope, contest) %> |
            <%= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_backend_scope_contest_path(@scope, contest) %> |
            <%= link_to "#{t("web-app-theme.delete", :default => "Delete")}", backend_scope_contest_path(@scope, contest), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}" %>
          </td>
        </tr>          
        <% end -%>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>
        
      </div>
    </div>
  </div>
</div>

<%= content_for :sidebar, render(:partial => 'sidebar') -%>
