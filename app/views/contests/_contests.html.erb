<%= render 'contests/sidebar' %>
<div class="contests">
  <div class="right_side">
    <%= link_to t('.new'), new_contest_path(locale:assigned_locale), class:'button' %>
  </div>
  <%= render @contest %>
</div>
<div class="clear"></div>
<%= render @contest.collections %>